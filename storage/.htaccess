# =========================================================
# AutoCare - storage/.htaccess
# Block all HTTP access to storage (private data, backups, logs)
# =========================================================

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
